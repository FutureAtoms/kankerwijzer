# Backend

Minimal FastAPI scaffold for:
- source connectors
- provenance-aware retrieval
- Anthropic answer orchestration
- GraphRAG storage schemas
- evaluation scripts

Optional integrations:
- `uv sync --extra firecrawl`
- `uv sync --extra docs`
- `uv sync --extra anthropic`
- `uv sync --extra graph`
